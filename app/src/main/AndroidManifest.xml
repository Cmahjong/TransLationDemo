<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yj.demo.translationdemo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".TranslationOneActivity"
            android:label="转场动画1"
            android:theme="@style/Translation">
        </activity>
        <activity
            android:name=".TranslationTwoActivity"
            android:label="转场动画2"
            android:theme="@style/Translation">
        </activity>
        <activity android:name=".TranslationThreeActivity"  android:theme="@style/Translation">
        </activity>
        <activity android:name=".TranslationFourActivity"  android:theme="@style/Translation">
        </activity>
    </application>

</manifest>